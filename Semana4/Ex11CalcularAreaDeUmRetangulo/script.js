/*Crie uma arrowFunction que receba um objeto contendo altura e 
largura de um retângulo e retorne a área do retângulo. */

const areaRetangulo = (b, h) => b * h;

const area = areaRetangulo(50, 90);
console.log(`A área do retângulo é de ${area}cm²`)